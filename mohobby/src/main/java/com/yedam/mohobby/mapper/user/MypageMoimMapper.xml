<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.mohobby.mapper.user.MypageMoimMapper">


<!-- 유저가 공개중인 소모임 목록 -->
<select id="getProfileMoim" parameterType="String" resultType="com.yedam.mohobby.service.moim.MoimMemberVO">
select a.moim_name, a.moim_img, a.moim_catg, a.moim_region, a.reg_cnt
from moim a join moim_member b
on a.moim_id = b.moim_id
where b.member_id = #{memberId} and b.profile_yn = #{profileYn} 
</select>

<!-- 유저가 공개중인 소모임 목록 수정 -->
<update id="updateProfileMoim" parameterType="com.yedam.mohobby.service.moim.MoimMemberVO">
update moim_member set profile_yn = #{profileYn} 
where member_id = #{memberId} and moim_id = #{moimId}
</update>


<!-- 유저가 참여한 소모임 목록(카테고리별 조회) -->
<select id="getTakeMoim" parameterType="hashMap" resultType="com.yedam.mohobby.service.user.MypageMoimVO">
select a.moim_name, a.moim_img, a.moim_catg, a.moim_region, a.reg_cnt
from moim a join moim_member b
on a.moim_id = b.moim_id
where a.moim_catg = #{moimCatg} and b.member_id = #{memberId} and b.member_role = 0
</select>



<!-- 유저가 운영중인 소모임 목록(카테고리별 조회) -->
<select id="getManageMoim" parameterType="hashMap" resultType="com.yedam.mohobby.service.user.MypageMoimVO">
select a.moim_name, a.moim_img, a.moim_catg, a.moim_region, a.reg_cnt
from moim a join moim_member b
on a.moim_id = b.moim_id
where a.moim_catg = #{moimCatg} and b.member_id = #{memberId} and b.member_role = 1
</select>
</mapper>